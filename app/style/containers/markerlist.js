import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  calout: {
    width: 100,
    height: 100,
    backgroundColor: "#fff"
  },
  image: {
    width: 60,
    height: 60,
    flex: 1,
    borderRadius: 30
  }
});
